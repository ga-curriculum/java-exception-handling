<h1>
  <span class="headline">Java Exception Handling</span>
  <span class="subhead">Instructor Guide</span>
</h1>

## Answers for independent practice in Handling Exceptions microlesson
1. No. We can’t have `catch` or `finally` block without a `try` statement.
2. No. A `try` statement should have either `catch` block or `finally` block, it can have both blocks.
3. No. We can’t write any code between `try-catch-finally` blocks.
4. Yes. We can have multiple `catch` blocks, each handling different exception objects with a single try statement.

## Answers for independent practice in Generating Exceptions microlesson
1. [This link](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/lang/Exception.html) can be used as a reference for all the exception sub classes. If you deem that the students are ready to interpret this page, you can share it with them too.
